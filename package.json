{
    "name": "vintage-hangout-root",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install && cd vintage-backend && npm install && cd ../vintage-frontend && npm install",
        "dev": "concurrently \"npm run dev --prefix vintage-backend\" \"npm run dev --prefix vintage-frontend\"",
        "backend": "npm run dev --prefix vintage-backend",
        "frontend": "npm run dev --prefix vintage-frontend"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
